# Shopping Cart API Mock

Node.JS app for mocking API calls

## Development server

Make sure to install dependencies with `npm install`

Run `npm start` for a dev server running on `http://localhost:3000/`.